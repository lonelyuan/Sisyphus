# Sisyphus 项目约定

## 项目身份

这是西西弗斯计划应用端与 Core 的开发仓库。项目既包含正常软件开发，也承载“用户任意输入 → 意图拆解 → Core 状态同步”的产品行为。

## 意图工程

- 当用户是在使用西西弗斯助手，或任务涉及意图识别、capture、候选确认、任务/提醒/目标/习惯/检测规则与 Core 同步时，使用 `sisyphus` skill。
- 用户态输入先原样 capture，再分类并同步最小必要状态；不制造任务海，不猜测时间和优先级。
- 普通仓库开发指令（修 bug、写代码、跑测试、改文档）不是用户生活输入，不得 capture；按正常工程流程完成。
- 修改意图路由、Core 数据模型或 MCP 工具时，把 `sisyphus` skill 当作产品契约，并同步更新相关引用文档和测试。

## 知识工程模式：off

`knowledge_reflection: off`

- 本项目不执行 `knowledge-engine` 后反思，不在回答末尾询问是否沉淀，也不把开发对话写入 Obsidian 知识库。
- 陈述性材料作为 Sisyphus 的 `note` 意图进入 Core，而不是知识卡片。
- 即使全局规则启用了弱反思，本项目规则也明确覆盖为关闭。

